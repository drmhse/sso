# Phase 2 identity-security evidence index

Status: active readiness tracker  
Scope: Phase 2 of [`PRODUCTION_READINESS.md`](../../PRODUCTION_READINESS.md)  
Last source review: 2026-07-14

This is the canonical index for incomplete identity-security and protocol
evidence. `PROJECT_STATUS.md` states supported behavior,
`PRODUCTION_READINESS.md` defines the release gate, and the security architecture
and threat model describe current controls. Those documents link here instead
of maintaining competing backlog lists.

## Status and work classes

- **Done:** implemented and linked to repeatable evidence.
- **In progress:** implementation or repeatable evidence is incomplete.
- **Blocked by implementation:** qualification would be misleading until local
  behavior exists.
- **External qualification:** local fixtures may exist, but independent software,
  a release-candidate environment, or an assessor must produce the result.
- **Time-based:** evidence requires repeated successful operation over a stated
  period and cannot be completed by a one-time code change.

No row becomes **Done** from source review alone when its closure evidence calls
for runtime, interoperability, or independent results. Artifact links must name
the exact commit or image digest and the tested database/profile.

## Control and evidence matrix

| ID | Control or release claim | Work class | Current status | Closure evidence |
| --- | --- | --- | --- | --- |
| P2-01 | Versioned threat model and security architecture | Local review plus maintainer approval | In progress | Security and Backend approval recorded against the current documents; architecture-change review rule linked from contribution guidance. |
| P2-02 | Runtime protocol metadata matches implemented behavior | Local | Done | `api/crates/authos-core/src/runtime_metadata.rs` route tests prove only device-code, token-exchange, JWT-bearer, and ID-JAG surfaces are advertised; standard OIDC/RFC 8414 discovery returns `404`. |
| P2-03 | OAuth authorization-code/OpenID Connect provider | Local implementation, then external qualification | Blocked by implementation; unsupported | Standards authorization endpoint, exact redirect and client policy, one-time code plus PKCE, nonce, distinct ID-token claims/type and issuance, accurate metadata, negative suite, then applicable conformance results. |
| P2-04 | Every tenant-scoped resource denies cross-tenant access | Local, three-database runtime | In progress — structural matrix and highest-risk SQLite fixes implemented | `docs/security/tenant-isolation-matrix.json` is CI-checked against all 51 SeaORM entities, the prose inventory, 178 route paths from `router.rs` and `main.rs`, and named SQLite test functions; it intentionally records 28 open critical/high gaps. Local regressions bind billing webhook metadata to customer/org/service/plan/user membership, scope service-principal identity queries by both org and service, separate same-email platform and tenant users, deny cross-user connected-account/device/passkey mutations without changing target state, bind provider grants to an owned matching account, give provider-request completion one user-bound winner before grant mutation, deny service keys for non-active organizations, recheck platform-owner status from the database, place every inventoried platform route behind the current-database owner boundary, bound public health/JWKS/metrics responses, honor scoped audit/billing capabilities, and use compound parent/child mutations for API keys, plans, domain routes, upstream providers, and SCIM tokens while preserving denied other-tenant state. Suspended organizations are rejected for risk policy, billing-credential, webhook, SIEM, domain-route, upstream-provider, and SCIM-token operations. Closure still requires method/actor/identifier coverage for every route, live PostgreSQL/MySQL parity, multi-replica timing, deployment proxy checks, and zero remaining critical/high matrix gaps. |
| P2-05 | Token profiles cannot be confused | Local | In progress — signed profile separation implemented | `api/crates/authos-crypto/src/crypto/jwt.rs` emits and validates distinct JOSE `typ` plus signed `token_use` for management, external-resource, MFA pre-authentication, impersonation, and ID-JAG tokens. The local matrix rejects wrong type/use/audience/issuer/actor/`kid`/algorithm and cross-profile validators. Closure still requires end-to-end revoked/missing-session behavior for every profile on all databases and an explicit upgrade note that legacy JWT sessions must reauthenticate. |
| P2-06 | Refresh-token theft and replay response | Local, three-database runtime | In progress — local SQLite hardening implemented | `api/crates/authos-crypto/src/crypto/refresh_tokens.rs` generates 256-bit opaque values; `api/crates/authos-store/src/store/sessions.rs` stores only SHA-256 hashes and atomically rotates with consumed-ancestor history and family revocation. SQLite tests cover database-dump canaries, concurrent one-winner/reuse response, expiry cleanup, refresh expiry, and logout. `api/migration/src/m20260714_000001_hash_refresh_tokens.rs` clears legacy plaintext values, forcing reauthentication. Closure still requires PostgreSQL/MySQL runtime migration and race evidence, plus distributed multi-replica qualification. |
| P2-07 | Application secrets are encrypted and rotatable | Local plus recovery exercise | In progress — V2 AES-GCM envelopes authenticate key ID plus table/record/field context. A default-dry-run operator command inventories 14 values across ten tables, transactionally rewrites bounded batches with compare-and-swap, repairs key-ID metadata, migrates SIEM/webhook and OAuth compatibility plaintext, and resumes idempotently. Encrypted startup repeats the inventory read-only and refuses API/worker/bootstrap startup until maintenance rewrap is complete; runtime rejects plaintext compatibility tokens. SQLite tests cover dry-run, interruption/resume, idempotence, exact identity/account encrypted-field contexts, context swaps, conflicts, ambiguity, tamper rollback, restored-copy reads, rewrap, report canaries, and backup-dependent old-key refusal. The identity/connected-account plaintext columns are explicitly retained as a migration bridge; [removal criteria](../operations/key-rotation.md#identity-compatibility-column-retention-decision) require all-database/multi-writer, zero-value retention-window, release-candidate restore/retirement, and rollback evidence. | Qualify on PostgreSQL/MySQL and multiple writers; complete the zero-compatibility-value backup window and release-candidate old-key retirement drill before column removal; exercise secret-manager integration. |
| P2-08 | JWT and SAML signing-key lifecycle | Local plus release qualification | In progress — local JWT and SAML overlap implemented | JWT issuance selects only the active key while validators/JWKS accept the bounded configured previous ring. SAML rotation signs only with one valid active key and publishes the former certificate for seven days, capped at two previous certificates; metadata excludes expired/retired material, configuration deletion retires all, authenticated immediate overlap retirement is idempotent, and status reports near-expiry/expiry. SQLite migration/store/handler tests cover rollover, concurrency, metadata ordering, retirement, expiry, and authority. Closure still requires release-candidate JWT/SAML rollover through real verifier/SP caches, PostgreSQL/MySQL and multi-replica runtime evidence, restored-backup behavior, wired expiry alerts, emergency-compromise drills, and at least three independent SAML SPs. |
| P2-09 | Security audit durability and secret/PII redaction | Local runtime | In progress — login, organization, MFA, and platform actor calls persist a database outbox row before returning success. Transaction-aware enqueue now couples the highest-risk API-key, member/role/ownership, billing-credential, and impersonation-session mutations to their success events. Startup/periodic reconciliation uses bounded batches, same-database target-insert/outbox-delete transactions, exact duplicate replay, coded backoff, and observable dead-letter state. SQLite tests cover paired rollback, late failure ordering, crash/restart, closed wake channels, enqueue outage, all four kinds, duplicate replay, bounded work, retry exhaustion, poison payloads, and redacted error state. CI rejects credential/email tracing arguments. | Classify and convert the [44 remaining caller/direct paths](../security/audit-transaction-inventory.md); add claim/lease or qualify multiple reconcilers; add operator requeue/alerting and runtime secret canaries; prove PostgreSQL/MySQL outage/restart and reconciliation behavior. |
| P2-10 | Password, recovery, MFA, WebAuthn, session, bootstrap, and impersonation abuse resistance | Local and multi-replica qualification | In progress — major local SQLite abuse controls evidenced | File-backed SQLite races prove exactly one winner for password-reset, email-verification, magic-link, OAuth callback-state, WebAuthn challenge, bootstrap-login, and client-bound authorized device-code consumption. Magic-link issuance/verification require active bound tenants, exact tenant-user/service context, and validated callback bindings before consume; consume/session/device creation is transactional and consume-time authority is rechecked. OAuth/SAML callbacks require the atomic winner. Device MFA pre-authentication tokens cryptographically bind the exact device-code ID; ordinary MFA completion transactionally couples live user/tenant/service/resource checks, device authorization, pre-auth/backup-code claims, session creation, and success audit. Refresh rotation revalidates the non-deleted user, active tenant, exact membership/service identity, service parent, and registered resource in the rotation transaction while preserving replay-family revocation. Internal management audiences cannot be reclassified as external resources. Request-time password hashing/verification and MFA backup-code hashing/verification share a CPU-scaled Argon2 semaphore, blocking-pool offload, a two-second queue bound, and a 1,024-byte input bound; missing users still receive dummy verification. WebAuthn persists the complete library-updated credential with optimistic concurrency. Impersonation requires a live revocable session and current authority. Closure still requires deployment timing observations, bootstrap MFA/crash recovery, real-origin/RP/authenticator clone tests, measured overload behavior, live PostgreSQL/MySQL parity, and multi-replica distributed-rate/replay reports. |
| P2-11 | Outbound request, parser, decompression, archive, and crypto-work bounds | Local | In progress — bounded local high-risk paths | The [input/work inventory](../security/input-work-bounds.md) records current limits and gaps. OAuth refresh/user-info and configured Stripe/Polar APIs use pinned resolutions, no redirects, response bounds, and generic errors. GeoIP permits only MaxMind's documented HTTPS R2 redirect without forwarding the license key and bounds compressed/expanded/database bytes. SAML request decoding is capped at 262,144 encoded and 1,048,576 expanded bytes; AuthnRequest and LogoutRequest parsing also enforce 32 levels, 4,096 events, 64 attributes per element, and 512 total attributes, with deterministic boundary corpora. SAML RSA key generation and request-time Argon2 run in the blocking pool behind bounded semaphores. Closure still requires DNS-rebinding/proxy integration tests, maintained fuzz targets, broader non-SAML JSON/form/filter/archive corpora, XML-signature work bounds, and measured release-hardware thresholds. |
| P2-12 | Implemented OAuth device and enterprise extension conformance | External qualification | External qualification | Applicable profiles or an explicitly scoped independent protocol review against a release candidate, with raw sanitized configuration, pass/fail output, exclusions, commit, and image digest. |
| P2-13 | AuthOS SAML IdP interoperability | Local fixtures plus external implementations | External qualification | Adversarial signature/condition/replay/ACS tests and successful matrix against at least three independent SAML SP implementations, including metadata/certificate rollover. |
| P2-14 | Upstream SAML SP | Local implementation plus external implementations | Blocked by implementation; unsupported | Standards-based signed-response validation, unique signed IDs/wrapping defense, issuer/destination/audience/recipient/`InResponseTo`/time/replay checks, then successful matrix against at least three independent IdPs. |
| P2-15 | SCIM protocol behavior and tenant isolation | Local, three-database runtime | In progress — local SQLite route tests cover strict user/group filter errors, normalized pagination, unchanged PUT idempotency, strict user PATCH validation, transactional group PUT/PATCH negative cases, protected deprovisioning, cross-org user nondisclosure, cross-org group-member rejection, suspended-parent rejection at bearer authentication, and unchanged shared identities on denied PUT/PATCH. Global-field mutation uses organization-owned-user and live-membership predicates. | Repeat the route suite on PostgreSQL and MySQL; add deprovision/reprovision, concurrent and large-group replacement cases, token lifecycle, and a broader malformed-filter/PATCH corpus before closure. |
| P2-16 | SCIM client interoperability | External qualification | External qualification | Sanitized fixtures and successful results from at least two independent SCIM clients against the exact release candidate. |
| P2-17 | Severity rubric and vulnerability register | Local governance | In progress — disclosure-safe register populated | The rubric defines exploitability/tenant impact and release disposition. The register records the current RustSec maintenance warnings with dependency paths, owners, review triggers, and `v1.0` deadlines; local Rust/npm audits report no known vulnerability. Closure requires Security approval and an exact release-candidate sanitized export proving zero unresolved critical/high findings and non-expired medium dispositions. |
| P2-18 | Independent security assessment | External qualification | External qualification | Assessor, scope, exact release candidate, methodology, limitations, findings, remediation, and retest status published where disclosure is safe. |
| P2-19 | Sustained protected security CI | Time-based | Time-based | Twenty consecutive protected runs of the required security, tenant, database, protocol, migration, lint, and dependency gates, with failed negative controls shown to block merge. |

## Evidence hygiene

Evidence reports must contain:

1. The exact commit and, where applicable, container digest.
2. Database engine and version, topology, proxy assumptions, and configuration.
3. Protocol profile, client/peer implementation and version, sanitized fixtures,
   raw result location, summarized failures, and justified exclusions.
4. Test identifiers mapped to the relevant row above and to the
   [tenant-resource inventory](../security/tenant-resource-inventory.md) when
   authorization is involved.
5. Finding owner, severity, remediation state, due date, and retest link.

The matrix must be updated whenever a protocol claim, tenant resource, token
profile, cryptographic key, secret-storage path, or security-relevant outbound
integration changes.
